name: "Verify Main Candidate"

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read

# V1 is release-blocked. This workflow verifies main without publishing mutable images.
jobs:
  verify:
    uses: ./.github/workflows/verify.yml

name: "Verify Release Candidate"

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

# V1 is release-blocked. Tags are verified, but no registry credentials or tags are created.
jobs:
  verify:
    uses: ./.github/workflows/verify.yml
